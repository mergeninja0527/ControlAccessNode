# Server Configuration
PORT=3000
AMB=DEV

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_DABS=zkteco

# Email Configuration (for sending notifications)
EMAIL_USER=your-email@gmail.com
EMAIL_APP_PASSWORD=your-app-password

# Note: DB_PASS should be base64 encoded
# To encode a password, use: btoa('your-password') in browser console
# Or in Node.js: Buffer.from('your-password').toString('base64')
